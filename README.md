# go grpc protobuf

```bash
arifulislam@aidevstack:~/Downloads/devarifkhan/go-grpc-protobuf/REST_API_GO$ openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout key.pem -out cert.pem -config /etc/ssl/openssl.cnf
```